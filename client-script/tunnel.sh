#!/bin/bash
# reverse-ssh-gateway — standalone tunnel client script
# Establishes and maintains reverse SSH tunnels to the gateway VPS.
# Source config.env before running, or pass values via environment.
#
# Usage: ./tunnel.sh [start|stop|restart|status]
