---
# sample inventory — reverse-ssh-gateway
# Replace with your actual hosts.

gateway:
  hosts:
    vps01:
      ansible_host: YOUR_VPS_IP
      ansible_user: root
      ansible_python_interpreter: /usr/bin/python3

clients:
  hosts:
    homelab01:
      ansible_host: YOUR_NAT_HOST_IP
      ansible_user: root
      ansible_python_interpreter: /usr/bin/python3
