#!/bin/bash
# {{ ansible_managed }}
# reverse-ssh-gateway — tunnel client script (Ansible-managed)
# Establishes reverse SSH tunnels using configuration from ansible variables.
# This file is managed by Ansible — manual changes will be overwritten.
